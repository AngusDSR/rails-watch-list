<h1><%= @list.name %></h1>
<hr>
<div class="container">
  <div class="d-flex justify-content-end me-4">
    <%= link_to "Add movie to list", new_list_bookmark_path(@list), class: "btn btn-success"  %>
  </div>

  <% @list.bookmarks.all.each do |bookmark| %>
    <%= bookmark.comment %>

  <% end %>
  <%= link_to "Back", root_path, class: "btn btn-primary" %>
</div>
